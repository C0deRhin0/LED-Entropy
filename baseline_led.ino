// Phase 0 Day 2 - Baseline LED Mapping
// Declares the PWM-capable LED pins for the base array.

const int kLedPins[] = {3, 5, 6, 9, 10};
const int kLedCount = sizeof(kLedPins) / sizeof(kLedPins[0]);

void setup() {}

void loop() {}
